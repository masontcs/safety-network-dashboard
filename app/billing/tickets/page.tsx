import TicketsClient from '@/components/billing/TicketsClient'

// Auth, role gate and shell live in app/billing/layout.tsx.
export default function BillingTicketsPage() {
  return <TicketsClient />
}
