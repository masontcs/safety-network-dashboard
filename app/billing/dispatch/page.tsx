import DispatchClient from '@/components/billing/DispatchClient'

// Auth, role gate and shell live in app/billing/layout.tsx.
export default function BillingDispatchPage() {
  return <DispatchClient />
}
